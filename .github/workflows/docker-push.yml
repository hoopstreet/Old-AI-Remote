name: Docker Push
on: [push]
jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Push to DockerHub
        run: echo 'Pushing to DockerHub...'