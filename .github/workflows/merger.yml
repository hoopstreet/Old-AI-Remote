# Merger Workflow
name: Merger

on:
  workflow_run:
    workflows: ["Task Runner"]
    types:
      - completed

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Merge Changes
        run: |
          # Your logic to merge changes