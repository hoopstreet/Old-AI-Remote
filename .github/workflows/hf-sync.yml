name: Hugging Face Sync

on:
  push:
    tags:
      - 'v*'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Sync to Hugging Face
        run: huggingface-cli repo create mymodel && huggingface-cli repo upload mymodel --path ./model