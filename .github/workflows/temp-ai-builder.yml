name: TEMPORARY BUILDER SWARM

on:
  push:
    paths:
      - "convo.md"
      - "convo2.md"
      - "Temporary Builder/memory/**"

jobs:
  swarm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run DAG Brain
        run: |
          echo "🧠 SWARM TRIGGERED"
          node "Temporary Builder/Builder/runner.js" || true
