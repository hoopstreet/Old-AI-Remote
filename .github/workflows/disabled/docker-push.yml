name: Docker Push
on: [push]
jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Push Docker image
        run: echo 'Pushing Docker image...'