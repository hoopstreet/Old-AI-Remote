# Task Runner Workflow
name: Task Runner

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Execute Task
        run: |
          # Execute your task logic based on Task.md