# 🧠 TEMP BUILDER MEMORY RULES

ROOT RULE:
- Only .github/workflows/temp-ai-builder.yml is allowed workflow

TEMP BUILDER RULE:
- ALL generated AI logic must stay inside Temporary Builder/

PROJECT RULE:
- convo.md + convo2.md = ROOT project specification input

OUTPUT RULE:
- Real apps go to root repo
- Internal logic stays in Temporary Builder/

SAFETY:
- No duplicate workflows allowed
- No multi-agent workflow duplication
